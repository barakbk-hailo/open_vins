cmake-build-*
build
Build
*.*~
.idea
doxgen_generated
*.swp
*.swo
doc
.vscode

# Parameter-sweep template regenerated by run_timing_sweep.sh each invocation
# (cp "$BASE_CONFIG" "$TMP_CONFIG" — see the openvins-ros2-workspace parent)
config/euroc_mav/estimator_config_sweep.yaml
